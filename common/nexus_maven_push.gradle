apply plugin: 'maven'

configurations {
    deployerJars
}

repositories {
    mavenCentral()
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "http://192.168.1.101:8081/repository/hrz/") {
                authentication(userName: "admin", password: "admin123")
            }
            pom.project {
                name 'common'
                version '1.0.1'
                artifactId 'common'
                groupId 'com.hdyl.hrz'
                packaging 'aar'
                description 'library for android'
            }
        }
    }
}

